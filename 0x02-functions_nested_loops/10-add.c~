#include "main.h"
#define COMMA 44
#define SPACE 32
#define TIMES 9



/**
 * print_int - function to print integers using _putchar
 * @n: number to be printed
 * prints integers to stdout.
 * Return: void
 */
void print_int(int n)
{

	if (n / 10)
	{
		print_int(n / 10);
	}
	_putchar((n % 10) + '0');
}
/**
 * add - function to add two integers
 * @x: integer to be added
 * @y: 2nd integer to be added.
 * Return: int, their sum.
 */
int add(int x, int y)
{
	return ((i + j));
}
