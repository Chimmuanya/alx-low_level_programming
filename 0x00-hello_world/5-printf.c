#include <stdio.h>
#include <string.h>
/**
 * main -program entry point.
 * prints a specific string to stdout using puts.
 * Return: 0 (Success)
 */
int main(void)
{
	char st[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", st);
	return (0);
}
